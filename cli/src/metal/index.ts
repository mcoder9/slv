import { Command } from '@cliffy'

// metal Command
export const metalCmd = new Command()
  .description('Metal command for Solana')
  .action(async () => {
    console.log('Coming soon...')
    return
  })
